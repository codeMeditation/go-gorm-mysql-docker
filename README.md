# go-gorm-mysql-docker
REST API with GO, GORM, MySQL and Docker

To set up local development env, run the following command in terminal:
```console
docker-compose up
```
To start the application:
```console
go run main.go
```